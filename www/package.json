{
  "name": "jsonl-analyser",
  "version": "1.0.0",
  "description": "JSONL file analyser with real-time updates",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:ts": "prettier --write src/**/*.ts"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "plotly.js-dist-min": "^2.27.0"
  }
}
